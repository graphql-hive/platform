#!/usr/bin/env node

// Suppress warnings from the CLI
process.removeAllListeners('warning');

const oclif = require('@oclif/core');

console.log(oclif.settings);
console.log('process.env["DEBUG"]', process.env['DEBUG']);

oclif
  .execute({ dir: __dirname })
  .catch(e => {
    throw e;
  })
  .then(() => {});
